# Deprecated

此目录用于暂存当前运行包不再直接需要的开发辅助文件、旧报告、临时 wheel 缓存和诊断输出。

这些内容不会进入正式离线发布包。若后续确认完全不需要，可以再整体删除。

保留在项目根目录的内容应尽量只包含：

- 系统运行入口与运维脚本
- `app/` 应用代码
- `python/` 便携 Python
- `data/` 数据目录
- `runtime_install/` Win7 离线运行库安装器
- 部署、使用、维护相关文档
